#!/usr/local/bin/chimp

use io;

readname {
    io.print ("What's your name?");
    ret io.readline();
}

main(argv) {
    name = readname();
    io.print (str("hello, ", name));
    io.print (argv);
}

