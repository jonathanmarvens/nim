use io;

fib n {
  if n > 1 {
    ret fib(n - 1) + fib(n - 2);
  }
  ret n;
}

main argv {
  var f3 = spawn me {
    io.print(fib(30));
  };

  var f2 = spawn me {
    io.print(fib(20));
  };

  var f1 = spawn me {
    io.print(fib(10));
  };

  f1.join();
  f2.join();
  f3.join();
}

