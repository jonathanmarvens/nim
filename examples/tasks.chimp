use io;

main argv {
  var task = spawn receiver {
    io.print("hello, from a background task");
    var m2 = receiver.recv();
    io.print(m2);
  };

  var m = msg("foo", 1);
  task.send(m);
  task.join();
}

