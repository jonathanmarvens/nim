use chimpunit


main argv {

	chimpunit.test("Bool construct integer", fn { |t|
		t.equals(true, bool(1))
		t.equals(false, bool(0))
		t.equals(false, bool(-1))
	})

	chimpunit.test("Bool construct string", fn { |t|
		t.equals(true, bool("Hello, World"))
		t.equals(false, bool(""))
	})

}
